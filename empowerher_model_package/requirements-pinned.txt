# Pinned requirements for Railway deployment
# Exact versions to avoid compatibility issues

# Core ML libraries
scikit-learn==1.0.2
pandas==1.3.5
numpy==1.21.6

# Web framework (exact compatible versions)
Flask==2.0.3
Werkzeug==2.0.3
Flask-CORS==3.0.10

# Model persistence
joblib==1.1.1

# Production server
gunicorn==20.1.0

# System dependencies (if needed)
# These will be handled by Railway automatically
